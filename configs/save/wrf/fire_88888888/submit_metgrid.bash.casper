#!/bin/bash

#PBS -N metgrid
#PBS -q casper
#PBS -l select=2:ncpus=32:mpiprocs=32
#PBS -l walltime=0:20:00
#PBS -A UUMM0004

# Use scratch for temporary files to avoid space limits in /tmp
export TMPDIR=/glade/derecho/scratch/$USER/temp
mkdir -p $TMPDIR

module load openmpi
module list

touch METGRID_BEG
mpirun -n 32 ./metgrid.exe
touch METGRID_END
